{
  "name": "ungrund_test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ ungrund_test",
    "create-local": "graph create --node http://localhost:8020/ ungrund_test",
    "remove-local": "graph remove --node http://localhost:8020/ ungrund_test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ungrund_test",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.51.2",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
